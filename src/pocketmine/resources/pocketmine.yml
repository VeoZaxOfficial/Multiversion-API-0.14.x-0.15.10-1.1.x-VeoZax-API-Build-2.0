# VeoZaxAPI Main Configuration File
# These settings cannot be included in server.properties
# Some settings are safe, others can break your server if modified incorrectly
# New settings/defaults won't appear automatically on this file when upgrading.

settings:
 #Three-letter language code for server-side localization
 #Check your language code on https://en.wikipedia.org/wiki/List_of_ISO_639-2_codes
 language: "eng"
 #Whether to send all strings translated to server locale or let the device handle them
 force-language: false
 #When server shut down, the players will get kicked and this is what will show on there screen.
 shutdown-message: "Server closed"
 #Allow listing plugins via Query
 query-plugins: true
 #Hide server software name/version from Query responses (privacy)
 query-hide-software: false
 #Show a console message when a plugin uses deprecated API methods
 deprecated-verbose: true
 #Enable plugin and core profiling by default
 enable-profiling: false
 #Will only add results when tick measurement is below or equal to given value (default 20)
 profile-report-trigger: 20
 #Number of AsyncTask workers.
 #Used for plugin asynchronous tasks, world generation, compression and web communication.
 #Set this approximately to your number of cores.
 #If set to auto, it'll try to detect the number of cores (or use 2)
 #PERFORMANCE: "auto" lets the server use all available cores for chunk gen/compression
 #instead of being capped at 4, which speeds up world loading and packet compression.
 async-workers: auto
 #Enables use of non-production, development builds. WARNING: DO NOT enable this unless you are sure you know what you are doing.
 enable-testing: false

memory:
 #Global soft memory limit in megabytes. Set to 0 to disable
 #This will trigger low-memory-triggers and fire an event to free memory when the usage goes over this
 global-limit: 0

 #Main thread soft memory limit in megabytes. Set to 0 to disable
 #This will trigger low-memory-triggers and fire an event to free memory when the usage goes over this
 main-limit: 0

 #Main thread hard memory limit in megabytes. Set to 0 to disable
 #This will stop the server when the limit is surpassed
 #PERFORMANCE: raised from 1024 - a limit that's too tight forces emergency GC
 #sweeps (big freezes) under normal load. Lower this back down if your host
 #actually has less RAM available.
 main-hard-limit: 2048

 #Period in ticks to check memory (default 1 second)
 check-rate: 20

 #Continue firing low-memory-triggers and event while on low memory
 continuous-trigger: true

 #Only if memory.continuous-trigger is enabled. Specifies the rate in memory.check-rate steps (default 30 seconds)
 continuous-trigger-rate: 30

 garbage-collection:
  #Period in ticks to fire the garbage collector manually (default 30 minutes), set to 0 to disable
  #This only affect the main thread. Other threads should fire their own collections
  #PERFORMANCE: shortened from 36000 (30 min) to 12000 (10 min). Smaller, more frequent
  #collections feel smoother than one huge collection every 30 minutes.
  period: 12000

  #Fire asynchronous tasks to collect garbage from workers
  collect-async-worker: true

  #Trigger on low memory
  low-memory-trigger: true

 max-chunks:
  #Limit of chunks to load per player, overrides chunk-sending.max-chunks
  trigger-limit: 28

  #Do chunk garbage collection on trigger
  trigger-chunk-collect: true

  #Trigger on low memory
  low-memory-trigger: true

 world-caches:
  #PERFORMANCE: enabled (was true/disabled). Keeping chunks cached avoids
  #recomputing them repeatedly and makes world access noticeably snappier,
  #at the cost of a bit more RAM.
  disable-chunk-cache: false
  low-memory-trigger: true


network:
 #Threshold for batching packets, in bytes. Only these packets will be compressed
 #Set to 0 to compress everything, -1 to disable.
 #PERFORMANCE: raised from 256 - small, frequent packets (movement, block updates) skip
 #compression entirely now, so they reach players faster with less CPU overhead per packet.
 batch-threshold: 512
 #Compression level used when sending batched packets. Higher = Uses More CPU, Less = More Bandwidth Usage
 #Kept low (2) on purpose - prioritizes CPU/responsiveness over bandwidth savings.
 compression-level: 2
 #Use AsyncTasks for compression. Adds half/one tick delay, less CPU load on main thread
 async-compression: true
 #Experimental, only for Windows. Tries to use UPnP to automatically port forward
 upnp-forwarding: false

debug:
 #If > 1, it will show debug messages in the console
 level: 1
 #Enables /status, /gc
 commands: true

player:
 #Choose whether to enable player data saving.
 save-player-data: true

level-settings:
 #The default format that levels will use when created
 default-format: anvil
 #If true, converts from a format that is not the default to the default format on load
 #NOTE: This is currently not implemented
 convert-format: false
 #Automatically change levels tick rate to maintain 20 ticks per second
 auto-tick-rate: false
 auto-tick-rate-limit: 20
 #Sets the base tick rate (1 = 20 ticks per second, 2 = 10 ticks per second, etc.)
 base-tick-rate: 1
 #Tick all players each tick even when other settings disallow this.
 always-tick-players: false

chunk-sending:
 #Amount of chunks sent to players per tick
 #PERFORMANCE: raised from 1 - world loads in visibly faster for players instead
 #of trickling in chunk-by-chunk.
 per-tick: 4
 #Amount of chunks sent around each player
 max-chunks: 32
 #Amount of chunks that need to be sent before spawning the player
 #PERFORMANCE: lowered from 28 - players spawn in almost immediately instead of
 #staring at a loading screen; remaining chunks stream in right after they spawn.
 spawn-threshold: 6
 #Save a serialized copy of the chunk in memory for faster sending
 #Useful in mostly-static worlds where lots of players join at the same time
 cache-chunks: true

chunk-ticking:
 #Max amount of chunks processed each tick
 #PERFORMANCE: raised from 16 - crops, redstone, and block ticks around players
 #keep up better, so the world feels alive/responsive instead of "catching up".
 per-tick: 32
 #Radius of chunks around a player to tick
 #Left at 3 on purpose - going higher scales CPU cost fast for little visible benefit.
 tick-radius: 3
 #PERFORMANCE NOTE: kept enabled. Without this, chunks loaded from disk that were
 #never light-populated (imported worlds, chunks from a previous crash, etc.)
 #never get repaired -- this can show up as flickering terrain/sky at the edges
 #of loaded chunks while moving, since the client keeps receiving inconsistent
 #light data for the same columns.
 light-updates: true
 clear-tick-list: true

chunk-generation:
 #Max. amount of chunks in the waiting queue to be generated
 #PERFORMANCE: raised from 2 - prevents "hitting a wall" of stutter when players
 #run/fly quickly into ungenerated terrain.
 queue-size: 8
 #Max. amount of chunks in the waiting queue to be populated
 population-queue-size: 8

ticks-per:
 animal-spawns: 400
 monster-spawns: 1
 #PERFORMANCE: spread out from 6000 (5 min) to 12000 (10 min) - autosaves briefly
 #pause the world while writing to disk, so saving less often means fewer of those pauses.
 autosave: 12000
 cache-cleanup: 900
#PERFORMANCE: trimmed slightly from defaults (70/15/5/15). Fewer live mobs means
#less AI/pathfinding work every tick, which is one of the biggest hidden CPU costs
#on busy servers. Raise these back up if you want denser mob spawns and have CPU to spare.
spawn-limits:
  monsters: 50
  animals: 10
  water-animals: 3
  ambient: 8

auto-report:
 #Send crash reports for processing
 enabled: false
 send-code: true
 send-settings: true
 send-phpinfo: false
 host: crash.pocketmine.net

anonymous-statistics:
 #Sends anonymous statistics for data aggregation, plugin usage tracking
 enabled: true
 host: stats.pocketmine.net

commands:
 #Here you can customize server commands
 #Specify command names to override the default set here.
 #If no custom value is defined for a command, the default will be used.
 #NOTE: Some commands cannot be disabled here, such as the important ones like /stop, /reload, etc.
 default: true
 #Set override values per command here
 #For example, uncommenting the below will disable /plugins and /version
 #version: false
 #plugins: false

aliases:
 #Examples:
 #showtheversion: version
 #savestop: [save-all, stop]

worlds:
 #These settings will override the generator set in server.properties and allows loading multiple levels
 #Example:
 #world:
 # seed: 404
 # generator: FLAT:2;7,59x1,3x3,2;1;decoration(treecount=80 grasscount=45)

# VeoZaxAPI Anti-cheat settings
anti-cheat:
 # Message shown when a player is kicked for suspected fly hacking
 fly-kick-message: "Kicked for suspected fly hacking"

# VeoZaxAPI specific settings
veozax:
 # API version — do not change
 api-version: "1.0.0"
 # Show VeoZaxAPI banner on startup
 show-banner: true
 # Log refused plugins (those without api: ["VeoZaxAPI"])
 log-refused-plugins: true
 # Grace period in seconds before timing out a lagging player connection
 # PERFORMANCE/FEEL: raised from 2.0 - brief network hiccups no longer disconnect
 # players, so a short lag spike doesn't feel like the server "kicked" them.
 reconnect-grace-period: 3.0
 # Max log file size in MB before rotation
 log-max-size-mb: 10
 # Max rotated log archives to keep
 log-max-archives: 5
